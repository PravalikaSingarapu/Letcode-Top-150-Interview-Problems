class Solution {
    public int lengthOfLastWord(String s) {
        s=s.trim();
        int counter=0;

        for(int index = s.length()-1; index>=0; index--){
            if(s.charAt(index) != ' ') counter++;
            else return counter++;

        }
        return counter;
        
    }
}
